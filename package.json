{
  "name": "bun-prisma-seed",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "migrate": "bunx prisma migrate dev --skip-seed",
    "db:seed": "bunx prisma db seed"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^5.15.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0"
  },
  "prisma": {
    "seed": "bun ./prisma/seed_failed.ts"
  }
}
